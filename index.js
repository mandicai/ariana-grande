// let svg = d3.select('#song-line-chart').append('svg')

// svg.append('g')
//     .call(xAxis)

// svg.append('g')
//     .call(yAxis)

// let bubbles = svg.append('g').attr('class', 'bubbles')

// bubbles.selectAll('.bubble')
//     .data(data)
//     .enter().append('circle')
//     .merge(bubbles)
//     .attr('r', 10)
//     .attr('cx', d => x(d.releaseDate))
//     .attr('cy', d => y(d.hits))
//     .attr('fill', (d, i) => colorScale(i))
//     .on('mousemove', function (d) {
//         console.log(d.song)
//     })
